# Edge Models

This package contains edge deployment artifacts for the drone swarm.

- `yolov8n.pt` — placeholder YOLOv8 detection weights for development.
- `tinyml_classifier.tflite` — placeholder TinyML classifier model.
- `model_registry.py` — central model registry and hot-swap manager.

> Replace the placeholder model files with production weights before deploying.
